ext.feature_deps = [:]
def versions = [:]

//@f1soft@ feature versions
versions.core = "0.0.1"
versions.library_a = "0.0.1"
versions.library_b = "0.0.1"
// @f1soft@

def deps = [:]

// also export the versions
deps.versions  = versions

ext.feature_deps = deps


